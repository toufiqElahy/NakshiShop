@model List<Cart>

<div class="section section-padding">
        <div class="container">
            <form class="cart-form" action="#">
                <table class="cart-wishlist-table table">
                    <thead>
                        <tr>
                            <th class="name" colspan="2">Product</th>
                            <th class="price">Price</th>
                            <th class="quantity">Quantity</th>
                            <th class="subtotal">Total</th>
                            <th class="remove">&nbsp;</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)

                {
                        <tr>
                            <td class="thumbnail"><a href="product-details.html"><img src="assets/images/product/cart-product-1.webp" alt="cart-product-1"></a></td>
                            <td class="name"> <a href="product-details.html">@item.Product.Catagory</a></td>
                            <td class="price"><span>@item.Product.Price</span></td>
                            <td class="quantity">
                                <div class="product-quantity">
                                    <span class="qty-btn minus"><i class="ti-minus"></i></span>
                                    <input type="text" class="input-qty" value="1">
                                    <span class="qty-btn plus"><i class="ti-plus"></i></span>
                                </div>
                            </td>
                            <td class="subtotal"><span>@item.Product.Price</span></td>
                            <td class="remove"><a href="/User/RemoveCart?cid=@item.Id" class="btn">×</a></td>
                        </tr>
                        }
                      
                    </tbody>
                </table>
             @*   <div class="row justify-content-between mb-n3">
                    <div class="col-auto mb-3">
                        <div class="cart-coupon">
                            <input type="text" placeholder="Enter your coupon code">
                            <button class="btn"><i class="fal fa-gift"></i></button>
                        </div>
                    </div>
                    <div class="col-auto">
                        <a class="btn btn-light btn-hover-dark mr-3 mb-3" href="shop.html">Continue Shopping</a>
                        <button class="btn btn-dark btn-outline-hover-dark mb-3">Update Cart</button>
                    </div>
                </div>*@
                 <div class="cart-totals mt-5">
                @*<h2 class="title">Cart totals <span class="amount">£242.00</span></h2>*@
               
                <a href="/User/Checkout" class="btn btn-dark btn-outline-hover-dark">Proceed to checkout</a>
            </div>
            </form>
           
        </div>

    </div>